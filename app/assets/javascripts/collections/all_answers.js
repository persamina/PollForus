PollForUs.Collections.AllAnswers = Backbone.Collection.extend({
  model: PollForUs.Models.Answer

  // parse: function(respAttrs, options) {
    //debugger
    //var parsedPolls = [];
    //respAttrs.forEach(function(poll) {
     // parsedPolls.push(new PollForUs.Models.Poll(poll));
    //})
    //return parsedPolls;
  //},


});
